# ─── Frontend (Vite) ───
# Set this in production to point to the backend machine's URL
# In dev, the Vite proxy handles /api → localhost:3001 so this can be omitted
# VITE_API_BASE=https://api.cashendar.yourdomain.com/api
